name: k8s-management-v2 integration workflow

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  kubernetes-repo-standards:
    uses:
      clubcedille/cedille-workflows/.github/workflows/kubernetes-repo-standards.yml@master
